*bit(비트) 란?
비트(bit) - 0과 1을 저장할 수 있는 최소 단위를 말한다.
1bit - 2가지 정보 / 2bit - 4가지 정보 / 2^n 정보 표현
bit의 단위 0 ~ 255
8bit = 2^8 = 256가지 정보

byte(바이트) - 8개의 비트가 모이면 바이트가된다. (나중에 혀니c 전자 계산기 일반 참고)
byte(1byte) < short(2byte) < int(4byte) < long(8byte)

*부동 소수점 방식 공부하기

*문자 자료형
문자 저장 시 유니코(문자 인코딩 방식)드 사용한다.

*논리 자료 형(boolean)

* 상수 = 선언하면서 초기화 해야한다.
